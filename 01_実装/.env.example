# このファイルを .env にコピーして値を入れる。.env は絶対にコミットしないこと。

# Jev(TypeSafe AI)の API キー。https://console.typesafe.ai/settings/keys で発行(早期アクセス中)。全モードで必須。
TYPESAFE_API_KEY=

# 即パット(IPAT)の認証情報。--mode prepare-only / live のときだけ必要。
IPAT_INET_ID=
IPAT_USER_NUMBER=
IPAT_PASSWORD=
IPAT_P_ARS=
